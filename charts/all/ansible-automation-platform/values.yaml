global:
  # if defined it's assumed to me an external ansible cluster
  ecp:
    aapURL: ''

secretStore:
  name: vault-backend
  kind: ClusterSecretStore

aapManifest:
  key: 'secret/data/global/aap-manifest'
controller:
  key: 'secret/data/global/aap-token'
