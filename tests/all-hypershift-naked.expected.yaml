---
# Source: hypershift/templates/eso-hypershift-aws-dns.yaml
#
# ---
# apiVersion: "external-secrets.io/v1beta1"
# kind: ExternalSecret
# metadata:
#   name: hypershift-eso-aws-dns
#   namespace: local-cluster
#   annotations:
#     argocd.argoproj.io/sync-wave: '5'
# spec:
#   refreshInterval: 15s
#   secretStoreRef:
#     name: vault-backend
#     kind: ClusterSecretStore
#   target:
#     name: hypershift-operator-external-dns-credential
#     template:
#       mergePolicy: Merge
#       type: Opaque
#       data:
#         provider:
#         domain-filter:
#         aws-access-key-id:
#         aws-secret-access-key:
#         region: 
#         bucket: 
#   dataFrom:
#   - extract:
#       key: secret/data/hub/awsCreds
#


# # oc patch mce multiclusterengine --type=merge -p '{"spec":{"overrides":{"components":[{"name":"hypershift-preview","enabled": true}]}}}' 

# # oc patch mce multiclusterengine --type=merge -p '{"spec":{"overrides":{"components":[{"name":"hypershift-local-hosting","enabled": true}]}}}'
---
# Source: hypershift/templates/eso-hypershift-aws.yaml
apiVersion: "external-secrets.io/v1beta1"
kind: ExternalSecret
metadata:
  name: hypershift-eso-aws
  namespace: cluster
  annotations:
    argocd.argoproj.io/sync-wave: '5'
spec:
  refreshInterval: 15s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: hypershift-operator-oidc-provider-s3-credentials
    template:
      mergePolicy: Merge
      type: Opaque
      data:
        region: 
        bucket: 
  dataFrom:
  - extract:
      key: secret/data/hub/awsCreds
---
# Source: hypershift/templates/eso-hypershift-pullsecret.yaml
apiVersion: "external-secrets.io/v1beta1"
kind: ExternalSecret
metadata:
  name: eso-hypershift-pull-secret
  namespace: cluster
  annotations:
    argocd.argoproj.io/sync-wave: '5'
spec:
  refreshInterval: 15s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: hypershift-pull-secret
    template:
      type: Opaque
  dataFrom:
  - extract:
      key: secret/data/global/ocpPull
---
# Source: hypershift/templates/mce.yaml
apiVersion: multicluster.openshift.io/v1
kind: MultiClusterEngine
metadata:
  name: multiclusterengine
spec:
  availabilityConfig: High
  overrides:
    components:
    - enabled: true
      name: local-cluster
    - enabled: true
      name: assisted-service
    - enabled: true
      name: cluster-lifecycle
    - enabled: true
      name: cluster-manager
    - enabled: true
      name: discovery
    - enabled: true
      name: hive
    - enabled: true
      name: server-foundation
    - enabled: true
      name: cluster-proxy-addon
    - enabled: true
      name: hypershift-local-hosting
    - enabled: true
      name: managedserviceaccount-preview
    - enabled: true
      name: hypershift-preview
    - enabled: true
      name: console-mce
  targetNamespace: multicluster-engine
  tolerations:
  - effect: NoSchedule
    key: node-role.kubernetes.io/infra
    operator: Exists
